V3 fixes: main grid uses Supabase server-side .range() paging with PAGE_SIZE=5000 so the browser does not load all leads at once. Bottom pager shows Prev/Next/page buttons and current page count. Edit modal state is now stable using an editingLead object instead of row index, so it should not flash open and close after table refresh. Single-click selects a row; double-click or Open/Edit Selected opens the edit window.
